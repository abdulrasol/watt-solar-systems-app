info:
  name: FeedbackController_updateFeedback
  type: http
  seq: 1
  tags:
    - Feedbacks

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/feedback/s:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "is_read": false
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/feedback/s:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "is_read": false
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
