info:
  name: CurrencyController_getDefaultCurrency
  type: http
  seq: 1
  tags:
    - Currencies

http:
  method: GET
  url: "{{baseUrl}}/api/v1/currency/default"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/currency/default"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
