info:
  name: CompanyTypesController_listCompanyTypes
  type: http
  seq: 1
  tags:
    - Company Types

http:
  method: GET
  url: "{{baseUrl}}/api/v1/company-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/company-types"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
