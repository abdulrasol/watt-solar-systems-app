info:
  name: SubscriptionController_createSubscription
  type: http
  seq: 2
  tags:
    - Subscriptions

http:
  method: POST
  url: "{{baseUrl}}/api/v1/subscriptions"
  body:
    type: json
    data: |-
      {
        "name": "",
        "duration_days": 0,
        "price": 0,
        "description": "",
        "is_active": false
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/subscriptions"
      method: POST
      body:
        type: json
        data: |-
          {
            "name": "",
            "duration_days": 0,
            "price": 0,
            "description": "",
            "is_active": false
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
