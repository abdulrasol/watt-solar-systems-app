info:
  name: CategoryController_updateCategory
  type: http
  seq: 1
  tags:
    - Categories

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "",
        "icon": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/categories/:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "name": "",
            "icon": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
