info:
  name: CategoryController_createCategory
  type: http
  seq: 2
  tags:
    - Categories

http:
  method: POST
  url: "{{baseUrl}}/api/v1/categories"
  body:
    type: json
    data: |-
      {
        "name": "",
        "icon": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/categories"
      method: POST
      body:
        type: json
        data: |-
          {
            "name": "",
            "icon": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
