info:
  name: AdminNotificationController_getStatistics
  type: http
  seq: 1
  tags:
    - Notifications - Admin

http:
  method: GET
  url: "{{baseUrl}}/api/v1/notification/statistics"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/notification/statistics"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
