info:
  name: DeviceController_subscribe
  type: http
  seq: 1
  tags:
    - Notifications - Devices

http:
  method: POST
  url: "{{baseUrl}}/api/v1/notification/subscribe"
  body:
    type: json
    data: |-
      {
        "token": "",
        "platform": "",
        "device_id": "",
        "app_version": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/notification/subscribe"
      method: POST
      body:
        type: json
        data: |-
          {
            "token": "",
            "platform": "",
            "device_id": "",
            "app_version": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
