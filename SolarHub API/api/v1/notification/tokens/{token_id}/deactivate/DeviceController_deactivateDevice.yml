info:
  name: DeviceController_deactivateDevice
  type: http
  seq: 1
  tags:
    - Notifications - Devices

http:
  method: POST
  url: "{{baseUrl}}/api/v1/notification/tokens/:token_id/deactivate"
  params:
    - name: token_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/notification/tokens/:token_id/deactivate"
      method: POST
      params:
        - name: token_id
          value: ""
          type: path
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
