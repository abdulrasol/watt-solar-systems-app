info:
  name: AdminNotificationController_sendUser
  type: http
  seq: 1
  tags:
    - Notifications - Admin

http:
  method: POST
  url: "{{baseUrl}}/api/v1/notification/send-user"
  body:
    type: json
    data: |-
      {
        "user_id": 0,
        "title": "",
        "body": "",
        "data": {}
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/notification/send-user"
      method: POST
      body:
        type: json
        data: |-
          {
            "user_id": 0,
            "title": "",
            "body": "",
            "data": {}
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
