info:
  name: DeviceController_unsubscribe
  type: http
  seq: 1
  tags:
    - Notifications - Devices

http:
  method: POST
  url: "{{baseUrl}}/api/v1/notification/unsubscribe"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/notification/unsubscribe"
      method: POST
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
