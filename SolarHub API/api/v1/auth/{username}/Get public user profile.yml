info:
  name: Get public user profile
  type: http
  seq: 1
  tags:
    - Auth & Users

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/:username"
  params:
    - name: username
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/auth/:username"
      method: GET
      params:
        - name: username
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
