info:
  name: List all users (Superuser only)
  type: http
  seq: 1
  tags:
    - Auth & Users

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth"
  params:
    - name: page
      value: ""
      type: query
    - name: limit
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/auth"
      method: GET
      params:
        - name: page
          value: ""
          type: query
        - name: limit
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
