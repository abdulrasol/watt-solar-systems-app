info:
  name: Validate password reset token
  type: http
  seq: 1
  tags:
    - Auth & Users

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/password-reset/validate-token"
  body:
    type: json
    data: |-
      {
        "token": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/auth/password-reset/validate-token"
      method: POST
      body:
        type: json
        data: |-
          {
            "token": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
