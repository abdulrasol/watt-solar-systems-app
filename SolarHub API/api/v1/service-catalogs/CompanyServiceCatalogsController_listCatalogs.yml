info:
  name: CompanyServiceCatalogsController_listCatalogs
  type: http
  seq: 1
  tags:
    - Company Service Catalogs

http:
  method: GET
  url: "{{baseUrl}}/api/v1/service-catalogs"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/service-catalogs"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
