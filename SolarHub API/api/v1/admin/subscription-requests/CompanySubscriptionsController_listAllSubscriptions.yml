info:
  name: CompanySubscriptionsController_listAllSubscriptions
  type: http
  seq: 1
  tags:
    - Company Subscription Requests

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/subscription-requests"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/admin/subscription-requests"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
