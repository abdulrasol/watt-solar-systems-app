info:
  name: CompanySubscriptionsController_reviewSubscription
  type: http
  seq: 1
  tags:
    - Company Subscription Requests

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/admin/subscription-requests/:request_id/review"
  params:
    - name: request_id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "status": "",
        "notes": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/admin/subscription-requests/:request_id/review"
      method: PUT
      params:
        - name: request_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "status": "",
            "notes": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
