info:
  name: CompanyPostersController_listAllPosters
  type: http
  seq: 1
  tags:
    - Company Posters

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/posters"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/admin/posters"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
