info:
  name: CountryController_updateCountry
  type: http
  seq: 1
  tags:
    - Countries

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/countries/:country_id"
  params:
    - name: country_id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "",
        "code": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/countries/:country_id"
      method: PUT
      params:
        - name: country_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "name": "",
            "code": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
