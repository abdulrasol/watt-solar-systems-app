info:
  name: CountryController_listCountries
  type: http
  seq: 1
  tags:
    - Countries

http:
  method: GET
  url: "{{baseUrl}}/api/v1/countries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/countries"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
