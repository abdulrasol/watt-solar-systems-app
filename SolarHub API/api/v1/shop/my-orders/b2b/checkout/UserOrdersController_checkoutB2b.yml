info:
  name: UserOrdersController_checkoutB2b
  type: http
  seq: 1
  tags:
    - User Purchases (B2C & B2B)

http:
  method: POST
  url: "{{baseUrl}}/api/v1/shop/my-orders/b2b/checkout"
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/shop/my-orders/b2b/checkout"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
