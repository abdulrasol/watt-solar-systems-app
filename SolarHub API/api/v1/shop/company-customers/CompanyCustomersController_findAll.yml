info:
  name: CompanyCustomersController_findAll
  type: http
  seq: 1
  tags:
    - Company Customers (CRM)

http:
  method: GET
  url: "{{baseUrl}}/api/v1/shop/company-customers"
  headers:
    - name: company-id
      value: ""
    - name: company-id
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/shop/company-customers"
      method: GET
      headers:
        - name: company-id
          value: ""
        - name: company-id
          value: ""
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
