info:
  name: AppController_getHello
  type: http
  seq: 1
  tags:
    - App

http:
  method: GET
  url: "{{baseUrl}}/api/v1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
