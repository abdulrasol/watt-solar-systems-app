info:
  name: ServiceTypesController_listServiceTypes
  type: http
  seq: 1
  tags:
    - Service Types

http:
  method: GET
  url: "{{baseUrl}}/api/v1/service-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/service-types"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
