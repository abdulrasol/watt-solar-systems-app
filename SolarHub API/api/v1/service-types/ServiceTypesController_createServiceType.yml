info:
  name: ServiceTypesController_createServiceType
  type: http
  seq: 2
  tags:
    - Service Types

http:
  method: POST
  url: "{{baseUrl}}/api/v1/service-types"
  body:
    type: json
    data: |-
      {
        "name": "",
        "description": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/service-types"
      method: POST
      body:
        type: json
        data: |-
          {
            "name": "",
            "description": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
