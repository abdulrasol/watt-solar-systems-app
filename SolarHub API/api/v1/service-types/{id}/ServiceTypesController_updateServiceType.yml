info:
  name: ServiceTypesController_updateServiceType
  type: http
  seq: 1
  tags:
    - Service Types

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/service-types/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "",
        "description": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/service-types/:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "name": "",
            "description": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
