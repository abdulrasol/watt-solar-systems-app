info:
  name: NotificationController_listNotifications
  type: http
  seq: 1
  tags:
    - Notifications

http:
  method: GET
  url: "{{baseUrl}}/api/v1/notifications"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/notifications"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
