info:
  name: CompanyOffersController_finishCompanyOffer
  type: http
  seq: 1
  tags:
    - Company Offers

http:
  method: POST
  url: "{{baseUrl}}/api/v1/offers/my-offers/:id/finish"
  headers:
    - name: company-id
      value: ""
      description: Company ID
    - name: company-id
      value: ""
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/offers/my-offers/:id/finish"
      method: POST
      headers:
        - name: company-id
          value: ""
          description: Company ID
        - name: company-id
          value: ""
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
