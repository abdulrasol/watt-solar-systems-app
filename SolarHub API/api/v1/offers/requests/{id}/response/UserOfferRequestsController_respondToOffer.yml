info:
  name: UserOfferRequestsController_respondToOffer
  type: http
  seq: 1
  tags:
    - User Offer Requests

http:
  method: POST
  url: "{{baseUrl}}/api/v1/offers/requests/:id/response"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/offers/requests/:id/response"
      method: POST
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: "{}"
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
