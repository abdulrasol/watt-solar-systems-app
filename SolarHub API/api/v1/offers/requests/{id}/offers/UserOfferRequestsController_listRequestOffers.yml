info:
  name: UserOfferRequestsController_listRequestOffers
  type: http
  seq: 1
  tags:
    - User Offer Requests

http:
  method: GET
  url: "{{baseUrl}}/api/v1/offers/requests/:id/offers"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/offers/requests/:id/offers"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
