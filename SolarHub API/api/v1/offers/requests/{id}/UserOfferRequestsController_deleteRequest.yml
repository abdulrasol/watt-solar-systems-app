info:
  name: UserOfferRequestsController_deleteRequest
  type: http
  seq: 3
  tags:
    - User Offer Requests

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/offers/requests/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/offers/requests/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
