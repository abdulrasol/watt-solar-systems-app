info:
  name: UserOfferRequestsController_listMyRequests
  type: http
  seq: 2
  tags:
    - User Offer Requests

http:
  method: GET
  url: "{{baseUrl}}/api/v1/offers/requests"
  params:
    - name: status
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/offers/requests"
      method: GET
      params:
        - name: status
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
