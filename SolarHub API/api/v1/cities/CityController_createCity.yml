info:
  name: CityController_createCity
  type: http
  seq: 2
  tags:
    - Cities

http:
  method: POST
  url: "{{baseUrl}}/api/v1/cities"
  body:
    type: json
    data: |-
      {
        "name": "",
        "country_id": 0,
        "code": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/cities"
      method: POST
      body:
        type: json
        data: |-
          {
            "name": "",
            "country_id": 0,
            "code": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
