info:
  name: ConfigController_listConfigs
  type: http
  seq: 1
  tags:
    - App Config

http:
  method: GET
  url: "{{baseUrl}}/api/v1/config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/config"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
