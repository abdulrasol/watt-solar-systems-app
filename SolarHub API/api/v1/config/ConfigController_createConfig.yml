info:
  name: ConfigController_createConfig
  type: http
  seq: 2
  tags:
    - App Config

http:
  method: POST
  url: "{{baseUrl}}/api/v1/config"
  body:
    type: json
    data: |-
      {
        "key": "",
        "value": false,
        "description": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/config"
      method: POST
      body:
        type: json
        data: |-
          {
            "key": "",
            "value": false,
            "description": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
