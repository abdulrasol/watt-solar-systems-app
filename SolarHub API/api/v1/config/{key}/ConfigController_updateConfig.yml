info:
  name: ConfigController_updateConfig
  type: http
  seq: 1
  tags:
    - App Config

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/config/:key"
  params:
    - name: key
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "value": false,
        "description": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/config/:key"
      method: PUT
      params:
        - name: key
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "value": false,
            "description": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
