info:
  name: ConfigController_deleteConfig
  type: http
  seq: 2
  tags:
    - App Config

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/config/:key"
  params:
    - name: key
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/config/:key"
      method: DELETE
      params:
        - name: key
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
