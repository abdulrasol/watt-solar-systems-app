info:
  name: CompanyServicesController_deleteService
  type: http
  seq: 2
  tags:
    - Company Services

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/companies/:company_id/services/:service_id"
  params:
    - name: company_id
      value: ""
      type: path
    - name: service_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/companies/:company_id/services/:service_id"
      method: DELETE
      params:
        - name: company_id
          value: ""
          type: path
        - name: service_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
