info:
  name: CompanyServicesController_createService
  type: http
  seq: 2
  tags:
    - Company Services

http:
  method: POST
  url: "{{baseUrl}}/api/v1/companies/:company_id/services"
  params:
    - name: company_id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "title": "",
        "price": 0,
        "description": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/companies/:company_id/services"
      method: POST
      params:
        - name: company_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "title": "",
            "price": 0,
            "description": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
