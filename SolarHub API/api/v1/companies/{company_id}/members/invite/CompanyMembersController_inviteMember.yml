info:
  name: CompanyMembersController_inviteMember
  type: http
  seq: 1
  tags:
    - Company Members

http:
  method: POST
  url: "{{baseUrl}}/api/v1/companies/:company_id/members/invite"
  params:
    - name: company_id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "email": "",
        "role": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/companies/:company_id/members/invite"
      method: POST
      params:
        - name: company_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "email": "",
            "role": ""
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
