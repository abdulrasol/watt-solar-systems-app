info:
  name: CompanyMembersController_removeMember
  type: http
  seq: 1
  tags:
    - Company Members

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/companies/:company_id/members/:member_id"
  params:
    - name: company_id
      value: ""
      type: path
    - name: member_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/companies/:company_id/members/:member_id"
      method: DELETE
      params:
        - name: company_id
          value: ""
          type: path
        - name: member_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
