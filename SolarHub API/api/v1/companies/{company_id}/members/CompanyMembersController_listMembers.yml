info:
  name: CompanyMembersController_listMembers
  type: http
  seq: 1
  tags:
    - Company Members

http:
  method: GET
  url: "{{baseUrl}}/api/v1/companies/:company_id/members"
  params:
    - name: company_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/companies/:company_id/members"
      method: GET
      params:
        - name: company_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
