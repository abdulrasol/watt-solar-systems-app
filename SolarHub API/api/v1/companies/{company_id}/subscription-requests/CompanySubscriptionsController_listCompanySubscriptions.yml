info:
  name: CompanySubscriptionsController_listCompanySubscriptions
  type: http
  seq: 2
  tags:
    - Company Subscription Requests

http:
  method: GET
  url: "{{baseUrl}}/api/v1/companies/:company_id/subscription-requests"
  params:
    - name: company_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/companies/:company_id/subscription-requests"
      method: GET
      params:
        - name: company_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
