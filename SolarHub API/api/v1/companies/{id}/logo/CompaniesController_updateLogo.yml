info:
  name: CompaniesController_updateLogo
  type: http
  seq: 1
  tags:
    - Companies

http:
  method: POST
  url: "{{baseUrl}}/api/v1/companies/:id/logo"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    request:
      url: "{{baseUrl}}/api/v1/companies/:id/logo"
      method: POST
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
