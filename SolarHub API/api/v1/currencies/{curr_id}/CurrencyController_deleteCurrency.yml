info:
  name: CurrencyController_deleteCurrency
  type: http
  seq: 2
  tags:
    - Currencies

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/currencies/:curr_id"
  params:
    - name: curr_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/currencies/:curr_id"
      method: DELETE
      params:
        - name: curr_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
