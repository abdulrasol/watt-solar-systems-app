info:
  name: CurrencyController_updateCurrency
  type: http
  seq: 1
  tags:
    - Currencies

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/currencies/:curr_id"
  params:
    - name: curr_id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "",
        "code": "",
        "symbol": "",
        "is_default": false
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/currencies/:curr_id"
      method: PUT
      params:
        - name: curr_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "name": "",
            "code": "",
            "symbol": "",
            "is_default": false
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
