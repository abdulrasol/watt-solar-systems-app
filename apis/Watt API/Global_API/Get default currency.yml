info:
  name: Get default currency
  type: http
  seq: 1
  tags:
    - Global API

http:
  method: GET
  url: "{{baseUrl}}/currency/default"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/currency/default"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""

docs: Returns the default currency for the application
