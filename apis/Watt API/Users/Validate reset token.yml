info:
  name: Validate reset token
  type: http
  seq: 6
  tags:
    - Users

http:
  method: POST
  url: "{{baseUrl}}/users/password-reset/validate-token"
  body:
    type: json
    data: |-
      {
        "token": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/users/password-reset/validate-token"
      method: POST
      body:
        type: json
        data: |-
          {
            "token": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: Validates if a password reset token is still valid
