info:
  name: Get user by username
  type: http
  seq: 10
  tags:
    - Users

http:
  method: GET
  url: "{{baseUrl}}/users/:username"
  params:
    - name: username
      value: ""
      type: path
      description: Username
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/users/:username"
      method: GET
      params:
        - name: username
          value: ""
          type: path
          description: Username
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: Fetch public profile of a user by username
