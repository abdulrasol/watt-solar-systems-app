info:
  name: ConfirmB2BReceipt
  type: http
  seq: 26
  tags:
    - Shop

http:
  method: POST
  url: "{{baseUrl}}/shop/b2b/my-orders/:order_id/confirm-receipt"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
  params:
    - name: order_id
      value: ""
      type: path
      description: order_id
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/shop/b2b/my-orders/:order_id/confirm-receipt"
      method: POST
      headers:
        - name: Authorization
          value: "{{apiKey}}"
      params:
        - name: order_id
          value: ""
          type: path
          description: order_id
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: Confirm receipt of B2B order and transfer stock
