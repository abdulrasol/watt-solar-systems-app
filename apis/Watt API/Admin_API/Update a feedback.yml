info:
  name: Update a feedback
  type: http
  seq: 27
  tags:
    - Admin API

http:
  method: PUT
  url: "{{baseUrl}}/api/admin/feedback/s:id"
  params:
    - name: id
      value: ""
      type: path
      description: Feedback ID
  body:
    type: json
    data: |-
      {
        "is_read": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/admin/feedback/s:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
          description: Feedback ID
      body:
        type: json
        data: |-
          {
            "is_read": false
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""

docs: Updates an existing feedback (e.g. mark as read)
