info:
  name: Get all app configs
  type: http
  seq: 7
  tags:
    - Admin API

http:
  method: GET
  url: "{{baseUrl}}/admin/config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/admin/config"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""

docs: Retrieves all global app configurations
