info:
  name: Delete an app config
  type: http
  seq: 25
  tags:
    - Admin API

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/config/:key"
  params:
    - name: key
      value: ""
      type: path
      description: Config Key
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/admin/config/:key"
      method: DELETE
      params:
        - name: key
          value: ""
          type: path
          description: Config Key
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: Deletes an existing global app configuration by key
