info:
  name: Update an app config
  type: http
  seq: 9
  tags:
    - Admin API

http:
  method: PUT
  url: "{{baseUrl}}/admin/config/:key"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
  params:
    - name: key
      value: ""
      type: path
      description: Config Key
  body:
    type: json
    data: |-
      {
        "description": "",
        "value": false
      }
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/admin/config/:key"
      method: PUT
      headers:
        - name: Authorization
          value: "{{apiKey}}"
      params:
        - name: key
          value: ""
          type: path
          description: Config Key
      body:
        type: json
        data: |-
          {
            "description": "",
            "value": false
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""

docs: Updates an existing global app configuration by key
