info:
  name: AdminGetCompanyMembers
  type: http
  seq: 18
  tags:
    - Admin Companies API

http:
  method: GET
  url: "{{baseUrl}}/admin/companies/:company_id/members"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
  params:
    - name: company_id
      value: ""
      type: path
      description: company_id
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/admin/companies/:company_id/members"
      method: GET
      headers:
        - name: Authorization
          value: "{{apiKey}}"
      params:
        - name: company_id
          value: ""
          type: path
          description: company_id
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: Get company members for admin
