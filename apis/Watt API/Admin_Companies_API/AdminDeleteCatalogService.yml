info:
  name: AdminDeleteCatalogService
  type: http
  seq: 5
  tags:
    - Admin Companies API

http:
  method: DELETE
  url: "{{baseUrl}}/admin/companies/catalog/services/:service_code"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
  params:
    - name: service_code
      value: ""
      type: path
      description: service_code
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/admin/companies/catalog/services/:service_code"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
      params:
        - name: service_code
          value: ""
          type: path
          description: service_code
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: AdminDeleteCatalogService
