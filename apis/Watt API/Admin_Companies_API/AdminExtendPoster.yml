info:
  name: AdminExtendPoster
  type: http
  seq: 7
  tags:
    - Admin Companies API

http:
  method: POST
  url: "{{baseUrl}}/admin/companies/posters/:poster_id/extend"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
  params:
    - name: poster_id
      value: ""
      type: path
      description: poster_id
  body:
    type: json
    data: |-
      {
        "expires_at": ""
      }
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/admin/companies/posters/:poster_id/extend"
      method: POST
      headers:
        - name: Authorization
          value: "{{apiKey}}"
      params:
        - name: poster_id
          value: ""
          type: path
          description: poster_id
      body:
        type: json
        data: |-
          {
            "expires_at": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: Extend a poster expiration
