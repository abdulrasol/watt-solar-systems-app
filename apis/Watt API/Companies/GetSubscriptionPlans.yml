info:
  name: GetSubscriptionPlans
  type: http
  seq: 6
  tags:
    - Companies

http:
  method: GET
  url: "{{baseUrl}}/companies/subscriptions"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/companies/subscriptions"
      method: GET
      headers:
        - name: Authorization
          value: "{{apiKey}}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: GetSubscriptionPlans
