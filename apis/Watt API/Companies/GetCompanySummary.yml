info:
  name: GetCompanySummary
  type: http
  seq: 9
  tags:
    - Companies

http:
  method: GET
  url: "{{baseUrl}}/companies/:company_id/summary"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
  params:
    - name: company_id
      value: ""
      type: path
      description: company_id
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/companies/:company_id/summary"
      method: GET
      headers:
        - name: Authorization
          value: "{{apiKey}}"
      params:
        - name: company_id
          value: ""
          type: path
          description: company_id
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "body": "",
            "error": false,
            "message": "",
            "message_user": "",
            "status": 0
          }

docs: GetCompanySummary
